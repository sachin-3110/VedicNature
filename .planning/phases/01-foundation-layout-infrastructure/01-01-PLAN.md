---
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements:
  - REQ-01
  - REQ-02
---

# Phase 1: Foundation & Layout Infrastructure Plan

## Objective
Initialize the Next.js App Router application with Tailwind CSS, configure custom fonts, and abstract the Navbar and Footer from the `design.html` context into persistent global layout components within `src/app/layout.tsx`.

## Tasks

### 1. Scaffold Next.js Application
<task>
<read_first>
- .planning/PROJECT.md
- src/app/layout.tsx (if exists)
</read_first>
<action>
Execute `npx -y create-next-app@latest . --typescript --eslint --tailwind --app --src-dir --import-alias "@/*" --use-npm` in the root directory. Ensure all prompts are bypassed using non-interactive mode flags. Install any peer dependencies (e.g., `lucide-react` if icons are updated from CDN, though we rely on Material Symbols for now).
</action>
<acceptance_criteria>
- `package.json` exists in `c:\Users\sachi\OneDrive\Desktop\RT Agency Work\vedicnature\Frontend`.
- `next` dependency is listed in `package.json`.
- `tailwind.config.ts` or `tailwind.config.js` exists.
- `src/app/layout.tsx` exists.
</acceptance_criteria>
</task>

### 2. Configure Tailwind and Fonts
<task>
<read_first>
- design.html
- tailwind.config.ts
- src/app/globals.css
</read_first>
<action>
1. Update `tailwind.config.ts` to include the specific theme colors defined in the `<script id="tailwind-config">` of `design.html` (e.g., `background: "#fcf9f3"`, `primary: "#5f5e5e"`, etc).
2. Update `src/app/globals.css` to add the `.material-symbols-outlined` CSS rules.
3. Import `Noto Serif` and `Manrope` using `next/font/google` in `src/app/layout.tsx` and apply their respective variables to the global body.
</action>
<acceptance_criteria>
- `tailwind.config.ts` contains the mapped color tokens from `design.html`.
- `src/app/globals.css` includes the `.material-symbols-outlined` ruleset.
- `src/app/layout.tsx` contains `import { Noto_Serif, Manrope } from "next/font/google";`.
</acceptance_criteria>
</task>

### 3. Implement Navbar Component
<task>
<read_first>
- design.html
- src/components/layout/Navbar.tsx
</read_first>
<action>
Extract the `<header>` block from `design.html` (lines 114 to 164) and implement it as a standalone server component `src/components/layout/Navbar.tsx`. Retain the existing Tailwind generic utility classes. Ensure the SVG/image source for the logo points to the existing Cloudfront link seamlessly or mapped locally if moved.
</action>
<acceptance_criteria>
- `src/components/layout/Navbar.tsx` exists.
- `src/components/layout/Navbar.tsx` exports a default function returning the `<header>` tag with navigational links.
</acceptance_criteria>
</task>

### 4. Implement Footer Component
<task>
<read_first>
- design.html
- src/components/layout/Footer.tsx
</read_first>
<action>
Extract the `<footer>` block from `design.html` (lines 567 to 658) and implement it as a standalone server component `src/components/layout/Footer.tsx`. Carry over the comprehensive Tailwind grid definitions.
</action>
<acceptance_criteria>
- `src/components/layout/Footer.tsx` exists.
- `src/components/layout/Footer.tsx` exports a default function returning the `<footer>` layout.
</acceptance_criteria>
</task>

### 5. Compose Global Layout
<task>
<read_first>
- src/app/layout.tsx
- src/components/layout/Navbar.tsx
- src/components/layout/Footer.tsx
</read_first>
<action>
Modify `src/app/layout.tsx` to render `<Navbar />` above the `{children}` and `<Footer />` below the `{children}` within the default `<body>` wrapper, utilizing the fonts established in Task 2. Also inject the Google Material Symbols CDN `<link>` into the layout if next/font strategies fail for icons.
</action>
<acceptance_criteria>
- `src/app/layout.tsx` imports both `Navbar` and `Footer`.
- `src/app/layout.tsx` renders `<Navbar />`, `{children}`, and `<Footer />`.
</acceptance_criteria>
</task>

## Verification
- Run `npm run build` to verify the Next.js build passes.
- Must verify that `design.html` tokens have fully superseded default Next.js templates.

### Must Haves
- The project runs a Next.js App Router setup with Tailwind explicitly hooked into the design configuration properties.
